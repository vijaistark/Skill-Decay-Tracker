# Skill Decay Tracker

A Flask web application for tracking technical skills and monitoring skill decay over time.

## Quick Start

```bash
python -m venv venv && venv\Scripts\activate && pip install -r requirements.txt && python app.py
```

Open http://127.0.0.1:5000

